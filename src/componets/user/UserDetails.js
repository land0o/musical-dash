import React, { Component } from 'react'
import Spotify from "spotify-web-api-js";
//1.get user info from Spotify and post to database
//2.a section that shows current users in session, session storage needed?
//3.structure for users in current session
//4. will need away to allow users to join a session in progress, an access code like Amber?

class UserDetails extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}

export default UserDetails
